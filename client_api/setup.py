"""
Setup v3online to function as python package


Then setup can be run with 'pip install -e .'
After which modules can be imported directly.

"""
from setuptools import setup, find_packages

setup(name="client_api", version="1.0", packages=find_packages())
